---
title: Learning Flexible Job Shop Scheduling under Limited Buffers and Material Kitting Constraints
title_zh: 有限缓冲区与物料齐套约束下的柔性作业车间调度学习
authors: "Shishun Zhang, Juzhan Xu, Yidan Fan, Chenyang Zhu, Ruizhen Hu, Yongjun Wang, Kai Xu"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.24180v1"
tags: ["query:ato"]
score: 8.0
evidence: 带有物料配套约束的柔性作业车间调度
tldr: 针对柔性作业车间调度（FJSP）中常被忽视的有限缓冲区和物料配套约束，本文提出一种基于深度强化学习（DRL）的调度方法。通过构建异构图网络对机器、工序和缓冲区进行全局建模，优化长序列调度中的托盘更换频率和缓冲区利用率。实验证明，该方法在完工时间和托盘更换次数上优于传统启发式及现有DRL算法，实现了求解质量与计算效率的平衡。
selection_source: fresh_fetch
motivation: 现有的FJSP研究往往忽略了有限缓冲区和物料配套等实际约束，导致生产效率受限。
method: 采用深度强化学习框架，利用异构图网络对生产要素间的复杂依赖进行建模，并通过高效的消息传递机制优化决策。
result: 在合成及真实生产线数据集上，该方法在缩短完工时间和减少托盘更换次数方面均表现优异。
conclusion: 该研究有效解决了带复杂约束的FJSP问题，为实际工业场景下的高效调度提供了兼顾性能与速度的解决方案。
---

## 摘要
柔性作业车间调度问题（FJSP）源于实际生产线，但在目前的 FJSP 研究中，一些实际约束往往被忽略或理想化，其中有限缓冲区问题对生产效率有着特殊的影响。为此，我们研究了一个更接近实际场景的扩展问题——具有有限缓冲区和物料齐套约束的柔性作业车间调度问题。近年来，深度强化学习（DRL）在调度任务中展现出巨大潜力。然而，在处理复杂的依赖关系和长期约束时，其状态建模能力仍然有限。为了解决这一问题，我们在 DRL 框架内利用异构图网络对全局状态进行建模。通过在机器、工序和缓冲区之间构建高效的消息传递，该网络专注于避免在长序列调度过程中可能导致频繁更换托盘的决策，从而有助于提高缓冲区利用率和整体决策质量。在合成数据集和真实生产线数据集上的实验结果表明，所提方法在最大完工时间（makespan）和托盘更换次数方面优于传统的启发式方法和先进的 DRL 方法，并在求解质量和计算成本之间取得了良好的平衡。此外，还提供了一个补充视频，展示了一个能够有效可视化生产线进展的仿真系统。

## 速览
**TLDR**：针对柔性作业车间调度（FJSP）中常被忽视的有限缓冲区和物料配套约束，本文提出一种基于深度强化学习（DRL）的调度方法。通过构建异构图网络对机器、工序和缓冲区进行全局建模，优化长序列调度中的托盘更换频率和缓冲区利用率。实验证明，该方法在完工时间和托盘更换次数上优于传统启发式及现有DRL算法，在求解质量与计算效率之间取得了良好平衡。 \
**Motivation**：传统FJSP研究常忽略有限缓冲区和物料配套等实际约束，导致其在真实生产场景中的效率受限。 \
**Method**：在深度强化学习框架下引入异构图网络，通过机器、工序和缓冲区间的消息传递实现全局状态建模与决策优化。 \
**Result**：实验结果显示，该方法在合成与真实数据集上的完工时间和托盘更换指标均优于传统启发式及先进的DRL方法。 \
**Conclusion**：本研究证明了异构图建模在处理复杂约束调度问题中的有效性，为提升实际生产线调度质量提供了新思路。

---

## Abstract
The Flexible Job Shop Scheduling Problem (FJSP) originates from real production lines, while some practical constraints are often ignored or idealized in current FJSP studies, among which the limited buffer problem has a particular impact on production efficiency. To this end, we study an extended problem that is closer to practical scenarios--the Flexible Job Shop Scheduling Problem with Limited Buffers and Material Kitting. In recent years, deep reinforcement learning (DRL) has demonstrated considerable potential in scheduling tasks. However, its capacity for state modeling remains limited when handling complex dependencies and long-term constraints. To address this, we leverage a heterogeneous graph network within the DRL framework to model the global state. By constructing efficient message passing among machines, operations, and buffers, the network focuses on avoiding decisions that may cause frequent pallet changes during long-sequence scheduling, thereby helping improve buffer utilization and overall decision quality. Experimental results on both synthetic and real production line datasets show that the proposed method outperforms traditional heuristics and advanced DRL methods in terms of makespan and pallet changes, and also achieves a good balance between solution quality and computational cost. Furthermore, a supplementary video is provided to showcase a simulation system that effectively visualizes the progression of the production line.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Flexible Job Shop Scheduling under Limited Buffers and Material Kitting Constraints》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**带有限缓冲区和物料齐套约束的柔性作业车间调度问题（FJSP-LB-MK）**。
*   **研究动机**：传统的柔性作业车间调度（FJSP）研究往往假设缓冲区无限或忽略物料配套逻辑。但在实际工业场景（如钢板切割与零件分拣）中，缓冲区（托盘）数量有限，且遵循“齐套”规则（即一个托盘只能存放同类零件）。
*   **核心挑战**：如果调度不当，会导致频繁的“换托盘”操作，产生巨大的时间开销，形成生产瓶颈。现有的深度强化学习（DRL）方法难以捕捉这种复杂的、长期的资源依赖关系。

### 2. 方法论
论文提出了一种基于**深度强化学习（DRL）与增强型异构图神经网络（HGNN）**的调度框架。
*   **核心思想**：将调度过程建模为马尔可夫决策过程（MDP），利用异构图捕捉机器、工序和缓冲区之间的动态依赖，通过“成本敏感”的消息传递机制引导模型避开高成本决策。
*   **关键技术细节**：
    *   **状态建模**：构建包含机器节点、工序节点和缓冲区节点的异构图。工序特征包含零件类别（One-hot）和预估换托盘成本（$SwEst$）。
    *   **选择性连接策略**：缓冲区节点仅与“零件分拣”相关的工序节点相连，减少信息冗余和噪声。
    *   **成本敏感传播（Cost-avoiding Strategy）**：在 GNN 聚合时，根据预估的换托盘成本动态调整边权重 $w_{ij} = \text{sigmoid}(\alpha \cdot SwEst)$。权重越高，表示该决策导致换托盘的风险越大，从而引导网络学习规避高成本操作。
    *   **奖励函数**：设计了双目标奖励 $r = \Delta C_{max} + \lambda \Delta P_{max}$，同时优化最大完工时间（Makespan）和总换托盘次数（Switches）。
*   **算法流程**：使用近端策略优化（PPO）算法进行训练，通过 Actor-Critic 架构输出工序-机器分配动作。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：涵盖 6 种规模（从 10任务×5机器 到 40任务×10机器），基于经典基准进行扩展。
    *   **真实生产线数据集**：采集自 4 条实际工业生产线（A、B、C、D），包含真实的机器映射、零件类别分布和加工时间。
*   **Benchmark（对比方法）**：
    *   **精确算法**：Google OR-Tools（约束编程求解器，设置 1800s 时限）。
    *   **启发式规则（PDRs）**：FIFO、MOR、SPT、MWR、LWR 等。
    *   **先进 DRL 方法**：Song 等人提出的基于 HGNN 的 FJSP 调度算法（2022）。

### 4. 资源与算力
*   **硬件环境**：NVIDIA GeForce RTX 3080 Ti GPU，Intel Core i9-10980XE CPU。
*   **训练细节**：使用 PPO 算法，设置最大迭代次数 10,000 次，每 20 次迭代更换一次训练实例。
*   **推理效率**：论文强调了 DRL 的秒级推理能力，相比 OR-Tools 的 1800s 求解时间，具有显著的实时性优势。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个合成规模和 4 个真实场景上进行了全面测试，每种方法在测试集上取平均表现。
*   **消融实验**：针对“关键状态特征（PS, Type, SwEst）”和“连接/传播策略（选择性连接、权重策略）”进行了多组对比实验。
*   **充分性评价**：实验设计较为充分，不仅对比了传统规则，还对比了 SOTA（当前最先进）的 DRL 模型和商业求解器。通过合成数据验证泛化性，通过真实数据验证实用性，客观地展示了模型在不同约束强度下的表现。

### 6. 主要结论与发现
*   **性能优越性**：在所有合成规模上，该方法在完工时间和换托盘次数上均优于传统规则和现有的 DRL 方法。
*   **泛化与扩展**：在小规模数据上训练的模型可以直接推广到大规模实例，且随着问题规模增大，领先优势更加明显。
*   **真实场景表现**：在真实生产线 C 中，该方法甚至超越了限时求解的 OR-Tools，证明了其在复杂约束下的决策质量。
*   **策略发现**：消融实验证明，“避祸策略（Cost-avoiding）”优于“趋利策略（Benefit-seeking）”，因为前者能更好地从全局角度缓解缓冲区拥堵。

### 7. 优点
*   **实际约束建模**：首次在 DRL 框架下深入探讨了“物料齐套”这一关键工业约束，填补了学术研究与实际生产间的鸿沟。
*   **异构图设计精巧**：通过选择性连接和动态权重，有效地将全局复杂的缓冲区约束分解为图上的局部可学习信号。
*   **效率与质量平衡**：在保持极短推理时间的同时，获得了接近甚至部分超越精确求解器的方案质量。

### 8. 不足与局限
*   **环境理想化**：实验未考虑机器故障（Breakdowns）、动态订单插入或加工时间随机性等现实扰动。
*   **多目标权衡**：在某些真实数据集上，为了优化完工时间，换托盘次数并未达到最低，说明在极端复杂的约束下，多目标之间的帕累托前沿仍有探索空间。
*   **应用限制**：目前主要针对零件分拣类场景，对于其他类型的缓冲区约束（如先进先出缓冲区）的适用性尚未验证。

（完）
